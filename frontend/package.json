{
  "name": "rendermate-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Grid visualization frontend — Vite + TypeScript",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
